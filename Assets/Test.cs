using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // Test, Can u  Git Pull? 12345
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
